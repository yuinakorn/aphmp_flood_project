Always use shadcn/ui components.

Never create custom modal, dropdown,
popover, select, toast, dialog,
sheet, tooltip, or command palette
if shadcn equivalent exists.

Prefer:
@/components/ui/*

Use Tailwind only for composition,
not reinventing primitives.
